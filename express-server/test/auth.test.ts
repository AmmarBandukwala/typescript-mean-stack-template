import { expect } from "chai";

describe("#", function () {
  // add a test hook
  beforeEach(function () {
    // ...some logic before each test is run
  });

  it("auth", function () {
    var isGood = true;
    expect(isGood).equal(true);
  });
});